diff --git a/configure b/configure
index f25a5aa..f6dc25a 100755
--- a/configure
+++ b/configure
@@ -11318,9 +11318,9 @@ fi
 
 if test "$NCURSES_CONFIG" != none ; then
 
-CPPFLAGS="$CPPFLAGS `$NCURSES_CONFIG --cflags`"
+CPPFLAGS="$CPPFLAGS `pkg-config --short-errors --print-errors --cflags "ncurses" 2>/dev/null`"
 
-cf_add_libs="`$NCURSES_CONFIG --libs`"
+cf_add_libs="`pkg-config --short-errors --print-errors --libs "ncurses" 2>/dev/null`"
 # Filter out duplicates - this happens with badly-designed ".pc" files...
 for cf_add_1lib in $LIBS
 do
@@ -11453,7 +11453,7 @@ cat >>confdefs.h <<EOF
 #define $cf_nculib_ROOT 1
 EOF
 
-cf_cv_ncurses_version=`$NCURSES_CONFIG --version`
+cf_cv_ncurses_version=`pkg-config --short-errors --print-errors --modversion "ncurses"`
 
 else
 
