diff --git a/src/events.c b/src/events.c
index 4b49171..1cd19b1 100644
--- a/src/events.c
+++ b/src/events.c
@@ -2665,7 +2665,9 @@ show_popup_cb (GtkWidget * widget, GdkEventButton * ev, gpointer data)
 {
     TRACE ("entering show_popup_cb");
 
+#if 0
     show_window_menu ((Client *) data, (gint) ev->x_root, (gint) ev->y_root, ev->button, ev->time);
+#endif
 
     return (TRUE);
 }
